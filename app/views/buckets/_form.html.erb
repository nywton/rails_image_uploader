<%= simple_form_for @bucket, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                buckets_path, :class => 'btn btn-default' %>
<% end %>
